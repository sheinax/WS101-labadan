"use strict";Object.defineProperty(exports, "__esModule", {value: true});

var _keywords = require('../parser/tokenizer/keywords');
var _types = require('../parser/tokenizer/types');







































/**
 * Get information about the class fields for this class, given a token processor pointing to the
 * open-brace at the start of the class.
 */
 function getClassInfo(
  rootTransformer,
  tokens,
  nameManager,
  disableESTransforms,
) {
  const snapshot = tokens.snapshot();

  const headerInfo = processClassHeader(tokens);

  let constructorInitializerStatements = [];
  const instanceInitializerNames = [];
  const staticInitializerNames = [];
  let constructorInsertPos = null;
  const fields = [];
  const rangesToRemove = [];

  const classContextId = tokens.currentToken().contextId;
  if (classContextId == null) {
    throw new Error("Expected non-null class context ID on class open-brace.");
  }

  tokens.nextToken();
  while (!tokens.matchesContextIdAndLabel(_types.TokenType.braceR, classContextId)) {
    if (tokens.matchesContextual(_keywords.ContextualKeyword._constructor) && !tokens.currentToken().isType) {
      ({constructorInitializerStatements, constructorInsertPos} = processConstructor(tokens));
    } else if (tokens.matches1(_types.TokenType.semi)) {
      if (!disableESTransforms) {
        rangesToRemove.push({start: tokens.currentIndex(), end: tokens.currentIndex() + 1});
      }
      tokens.nextToken();
    } else if (tokens.currentToken().isType) {
      tokens.nextToken();
    } else {
      // Either a method or a field. Skip to the identifier part.
      const statementStartIndex = tokens.currentIndex();
      let isStatic = false;
      let isESPrivate = false;
      let isDeclareOrAbstract = false;
      while (isAccessModifier(tokens.currentToken())) {
        if (tokens.matches1(_types.TokenType._static)) {
          isStatic = true;
        }
        if (tokens.matches1(_types.TokenType.hash)) {
          isESPrivate = true;
        }
        if (tokens.matches1(_types.TokenType._declare) || tokens.matches1(_types.TokenType._abstract)) {
          isDeclareOrAbstract = true;
        }
        tokens.nextToken();
      }
      if (isStatic && tokens.matches1(_types.TokenType.braceL)) {
        // This is a static block, so don't process it in any special way.
        skipToNextClassElement(tokens, classContextId);
        continue;
      }
      if (isESPrivate) {
        // Sucrase doesn't attempt to transpile private fields; just leave them as-is.
        skipToNextClassElement(tokens, classContextId);
        continue;
      }
      if (
        tokens.matchesContextual(_keywords.ContextualKeyword._constructor) &&
        !tokens.currentToken().isType
      ) {
        ({constructorInitializerStatements, constructorInsertPos} = processConstructor(tokens));
        continue;
      }

      const nameStartIndex = tokens.currentIndex();
      skipFieldName(tokens);
      if (tokens.matches1(_types.TokenType.lessThan) || tokens.matches1(_types.TokenType.parenL)) {
        // This is a method, so nothing to process.
        skipToNextClassElement(tokens, classContextId);
        continue;
      }
      // There might be a type annotation that we need to skip.
      while (tokens.currentToken().isType) {
        tokens.nextToken();
      }
      if (tokens.matches1(_types.TokenType.eq)) {
        const equalsIndex = tokens.currentIndex();
        // This is an initializer, so we need to wrap in an initializer method.
        const valueEnd = tokens.currentToken().rhsEndIndex;
        if (valueEnd == null) {
          throw new Error("Expected rhsEndIndex on class field assignment.");
        }
        tokens.nextToken();
        while (tokens.currentIndex() < valueEnd) {
          rootTransformer.processToken();
        }
        let initializerName;
        if (isStatic) {
          initializerName = nameManager.claimFreeName("__initStatic");
          staticInitializerNames.push(initializerName);
        } else {
          initializerName = nameManager.claimFreeName("__init"Wenn Sie zu dieser Seite zurückkehren, werden dort ausgeführte Aktionen möglicherweise wiederholt. Möchten Sie fortfahren?Artikel konnte nicht gefunden werden.Der angeforderte Artikel konnte nicht gefunden werden.Keine Daten gefundenIgnoriert, da entweder die Liste "aktivieren" oder "deaktivieren" fehlt.Ignoriert, weil die Deaktivierungsliste ein Muster enthält, das "*" gleicht. Dies entspricht dem Deaktivieren der Richtlinie.Ihr Administrator hat das Kopieren dieser Daten untersagt.Der Administrator hat das Dinosaurierspiel deaktiviert.Aktualisieren Sie die Seite, damit die zum Laden erforderlichen Daten erneut übermittelt werden.Überprüfen Sie Ihre Internetverbindung.Überprüfen Sie Netzwerkkabel, Modem und Router.Überprüfen Sie Ihre sicheren DNS-Einstellungen.Überprüfen Sie Ihre sicheren DNS-Einstellungen. Möglicherweise haben Sie einen sicheren DNS-Server konfiguriert, dessen Verbindungen fehlerhaft sind.Überprüfen Sie die DNS-Einstellungen. Die Seite konnte nicht geladen werden.Wenn Sie nicht sicher sind, was das bedeutet, wenden Sie sich an Ihre Organisation.Falls es bereits als Programm aufgelistet ist, das für den Zugriff auf das Netzwerk zugelassen ist, versuchen Sie,
      es aus der Liste zu entfernen und erneut hinzuzufügen.Wenn Sie einen Proxyserver verwenden:Überprüfen Sie Ihre Proxyeinstellungen. Unter Umständen müssen Sie Ihre Organisation fragen, ob der Proxyserver ausgeführt wird. Wenn Sie der Meinung sind, dass kein Proxyserver verwendet werden sollte, navigieren Sie zu $1.<a href="#buttons" onclick="javascript:portalSignin()">Anmeldung</a> im WLAN-NetzwerkÜberprüfen Sie die Richtlinien Ihrer Organisation.Rufen Sie <strong>edge://policy</strong> auf, um eine Liste der blockierten URLs und anderer von Ihrer Organisation erzwungener Richtlinien anzuzeigen.Nicht unterstütztes ProtokollClient und Server unterstützen keine gängige Version des SSL-Protokolls bzw. kein gängiges Verschlüsselungsverfahren.Besuchen Sie die <a jsvalues="href:originURL;.jstdata:$this" onmousedown="linkClicked(this.jstdata)">Startseite der Website</a>.Hmmm...diese Seite ist leider nicht erreichbarHmmm… Ihr Internetzugriff wird blockiertSie sind offlineDiese Anwendung fehlt oder ist beschädigtDer Entwicklungsserver für diese Anwendung ist nicht erreichbar.Die gespeicherte Kopie dieser Website kann nicht geladen werden.Ihre Verbindung wurde unterbrochen.Diese Seite von <span jscontent="hostName"></span> wurde nicht gefunden.<span jscontent="hostName"></span> ist gesperrt"<span jscontent="hostName"></span>"-Links werden blockiert.Es sieht so aus, als ob die Webseite unter <strong jscontent="failedUrl"></strong> Probleme hat oder sie wurde möglicherweise dauerhaft zu einer neuen Webadresse verschoben.Die Antwort von <strong jscontent="hostName"></strong> hat zu lange gedauertDie Verbindung wurde zurückgesetzt.Die Verbindung wurde offenbar von <strong jscontent="hostName"></strong> getrennt.<strong jscontent="hostName"></strong> ist derzeit nicht erreichbarEine Netzwerkänderung wurde festgestellt.<strong jscontent="hostName"></strong> hat eine Verbindung verweigert.Die IP-Adresse des Servers von <strong jscontent="hostName"></strong> konnte nicht gefunden werden.Diese Geschäfts-, Schul- oder Uni-Intranetsite hat dieselbe URL wie eine externe Webseite.
    <br /><br />
    Wenden Sie sich an Ihre Organisation.<strong jscontent="failedUrl"></strong> ist nicht erreichbar.Die Datei unter „<strong jscontent="failedUrl"></strong>” kann nicht gelesen werden. Möglicherweise wurde sie verschoben oder entfernt, oder der Zugriff wird aufgrund der Dateiberechtigungen blockiert.Die Verbindung wurde möglicherweise von einer Firewall oder Antivirensoftware blockiert.Bei dem Proxyserver ist ein Problem aufgetreten, oder die Adresse ist falsch.Die gespeicherte (zwischengespeicherte) Kopie dieser Website war nicht lesbar.Ihr Computer ist in den Standbymodus gewechselt.Für folgende Webadresse wurde keine Webseite gefunden: <strong jscontent="failedUrl"></strong>Sie wurde möglicherweise verschoben, bearbeitet oder gelöscht.<strong jscontent="hostName"></strong> hat Sie zu viele Male weitergeleitet.<strong jscontent="hostName"></strong> hat keine Daten gesendet.<strong jscontent="hostName"></strong> hat eine ungültige Antwort gesendet.<strong jscontent="hostName"></strong>’s <abbr id="dnsDefinition">DNS-Adresse</abbr> konnte nicht gefunden werden. Das Problem wird diagnostiziert.Zugriff auf <span jscontent="hostName"></span> wurde verweigertZugriff auf die Datei verweigertSie besitzen keine Benutzerrechte zum Anzeigen dieser Seite.Diese Seite wurde möglicherweise verschoben oder gelöscht.Diese Seite funktioniert im Moment nicht.Wenn das Problem weiterhin besteht, wenden Sie sich an den Besitzer der Website.<strong jscontent="hostName"></strong> kann diese Anfrage zurzeit nicht bearbeiten.Die Antwort von <strong jscontent="hostName"></strong> hat zu lange gedauert.<strong jscontent="hostName"></strong> erfüllt nicht unsere Sicherheitsstandards.<strong jscontent="hostName"></strong> verwendet ein nicht unterstütztes Protokoll.<strong jscontent="hostName"></strong> hat ihr Anmeldezertifikat nicht akzeptiert, oder es wurde kein Anmeldezertifikat bereitgestellt.Diese Seite wurde durch eine Erweiterung blockiertIhre Organisation lässt nicht zu, dass Sie diese Website anzeigenDieser Inhalt ist blockiert. Wenden Sie sich an den Besitzer der Website, um das Problem zu beheben.Damit diese Website korrekt angezeigt werden kann, müssen die zuvor eingegebenen Daten erneut gesendet werden. Dadurch wird jede Aktion wiederholt, die diese Seite zuvor ausgeführt hat.Die Verbindung wird überprüft…<a href="#buttons" onclick="toggleHelpBox()">Proxy-, Firewall- und DNS-Einstellungen überprüfen</a>.<a href="#buttons" onclick="toggleHelpBox()">Überprüfen Sie die Proxy-, Firewall- und sichere DNS-Konfiguration.</a><a href="#buttons" onclick="toggleHelpBox()">Firewall- und Antiviruskonfiguration überprüfen</a><a href="#buttons" onclick="toggleHelpBox()">Proxy und Firewall überprüfen</a><a href="#buttons" onclick="toggleHelpBox()">Proxyadresse überprüfen</a>Wenden Sie sich an Ihre Organisation.<a jsvalues="href:learnMoreUrl">Weitere Informationen</a> zu diesem Problem.<a jsvalues="href:learnMoreUrl">Versuchen Sie, Ihre Cookies zu löschen</a>.Netzwerkkabel, Modem und Router überprüfenVerbindung mit Ihrem Drahtlosnetzwerk wiederherstellenDeaktivieren Sie Ihre Erweiterungen.Überprüfen Sie, ob <span jscontent="hostName"></span> einen Tippfehler enthält.<a href="javascript:diagnoseErrors()" id="diagnose-link">Windows-Netzwerkdiagnose ausführen</a><a href="javascript:diagnoseErrors()" id="diagnose-link">Versuchen Sie, die Windows-Netzwerkdiagnose auszuführen</a>.Wenn die Rechtschreibung korrekt ist, <a href="javascript:diagnoseErrors()" id="diagnose-link">versuchen Sie, die Windows-Netzwerkdiagnose auszuführen</a>.Drücken Sie zum Wiedergeben die LEERTASTEDino-Spiel. Ein pixeliger Dinosaurier tröpft Kakteen und Pterodactyls aus, während er über eine desolate Landschaft läuft. Wenn Sie einen Audiohinweis hören, drücken Sie die LEERTASTE, um über Hindernisse zu springen.Dino-Spiel, spielenSpiel gestartet.Spiel zu Ende, Ihr Ergebnis ist: $1.Ihr bestes Ergebnis ist: $1.Springen!Langsamer startenMeinte